name = "echo"
version = "0.1.0"
description = "Simple Rust reference plugin that echoes command and args"
executable = "./run.sh"
capabilities = ["echo.run"]
allowed_tools = []
timeout_seconds = 60
env_allowlist = []
